export const THEME_BLOCK_HANDLE = 'tier_progress';

export const METAFIELD = {
  namespace: '$app',
  tierRulesKey: 'tier_rules',
  tierRulesType: 'json',
} as const;

export const FORM_INTENT = {
  save: 'save',
  setup: 'setup',
} as const;
