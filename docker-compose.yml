services:
  db:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: my_b2b_smart_app
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
