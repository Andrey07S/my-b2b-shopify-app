query CartInput {
  cart {
    cost {
      subtotalAmount {
        amount
      }
    }
    lines {
      id
    }
  }
  shop {
    metafield(namespace: "$app", key: "tier_rules") {
      jsonValue
    }
  }
  discount {
    discountClasses
  }
}
